'use client'
import React from 'react'

function AddToCard() {
  return (
    <div>
        <button onClick={() => console.log('click')}>Add To Card</button>
    </div>
  )
}

export default AddToCard